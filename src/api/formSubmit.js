export default async function formSubmit(formData) {
    // TODO: add the logic to submit the form data to API endpoint
    // return the response from the API endpoint
    console.log("form data", formData);
    //await new Promise(resolve => setTimeout(resolve, 1000));
    return formData;
    
}